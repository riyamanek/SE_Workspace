1.Declare a 1D array called dailySteps with 7 elements to store your step count for each day of the week, assign sample values, and print each value using a loop.

#include <stdio.h>
#include <conio.h>
void main(){
    
    int dailySteps[7]= {1000,1500,2800,2200,3200,3000,4500};
    
    for(int i=0;i<7;i++)
    {
        printf(" DailySteps count is %d\n ",dailySteps[i]);
    }
    
    getch();
}


2.Create a 2D array called playlistRatings to store ratings for 3 Spotify playlists over 5 days (rows = playlists, columns = days). 
  Fill it with sample numbers and print the ratings for the second playlist.

#include <stdio.h>
#include <conio.h>
void main(){
    int playlistRatings[3][5]=
    {
        {4,5,6,1,2},
        {3,2,1,5,3},
        {4,5,2,3,1}
        
    };
    
    printf("Print the rating for second playlist is :-\n");
    for(int j=0;j<5;j++)
    {
        printf("Second playlist rating is %d\n",playlistRatings[1][j]);
    
    }
    
    getch();
}



3.Build a function that takes a 1D array of 7 integers representing your daily Zomato order amounts and calculates the average spend for the week.
<br><br><em><strong>Hint:</strong> Use a loop to sum the values, then divide by the array length.</em>


#include <stdio.h>
#include <conio.h>

float ordersum(int dailyzomatoorder[])
{
    int sum=0;
    float spend=0;
    for(int i=0;i<7;i++)
    {
        sum+=dailyzomatoorder[i];
        spend=sum/7.0;
    }
     return spend;
}
void main(){
     int dailyzomatoorder[7]= {120,200,220,250,450,350,290};
     printf("average spend for the week %f",ordersum(dailyzomatoorder));
    
    getch();
}




4.Given a 2D array called cricketScores where each row represents an IPL match and columns represent runs scored by each team, 
write code to print the highest score from each match.

#include <stdio.h>
#include <conio.h>
void main()
{
    int cricketscores[3][3]=
    {
        {110,220,330},
        {240,156,320},
        {245,390,250}
    };
    
     ;
    for(int i=0;i<3;i++)
    {
        int highest=cricketscores[i][0];
        for(int j=1;j<3;j++)
        {
            if(cricketscores[i][j]>highest)
            {
                highest=cricketscores[i][j];
            }
        }
         printf("Highest score is %d\n",highest);
    }
   

    getch();
}